/*
 * Supabase recorded this version while the full migration was being verified inside an
 * explicit transaction that ended with ROLLBACK. Keep the version in the repository so
 * remote migration history and fresh preview databases agree. The effective data change is
 * the later 20260920210547 migration.
 */
select 1;
